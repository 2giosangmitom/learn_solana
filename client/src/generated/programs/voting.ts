/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getGlobalStateCodec,
  getPollAccountCodec,
  getVoteRecordCodec,
  type GlobalState,
  type GlobalStateArgs,
  type PollAccount,
  type PollAccountArgs,
  type VoteRecord,
  type VoteRecordArgs,
} from "../accounts";
import {
  getCreatePollInstructionAsync,
  getInitializeInstructionAsync,
  getVoteInstructionAsync,
  parseCreatePollInstruction,
  parseInitializeInstruction,
  parseVoteInstruction,
  type CreatePollAsyncInput,
  type InitializeAsyncInput,
  type ParsedCreatePollInstruction,
  type ParsedInitializeInstruction,
  type ParsedVoteInstruction,
  type VoteAsyncInput,
} from "../instructions";
import {
  findGlobalStatePda,
  findPollAccountPda,
  findVoteRecordPda,
} from "../pdas";

export const VOTING_PROGRAM_ADDRESS =
  "DXZ1gUBaHsj5cKAfRGZjqYigWF7wv1Js2aHKwKVYtUVA" as Address<"DXZ1gUBaHsj5cKAfRGZjqYigWF7wv1Js2aHKwKVYtUVA">;

export enum VotingAccount {
  GlobalState,
  PollAccount,
  VoteRecord,
}

export function identifyVotingAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VotingAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([163, 46, 74, 168, 216, 123, 133, 98]),
      ),
      0,
    )
  ) {
    return VotingAccount.GlobalState;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([109, 254, 117, 41, 232, 74, 172, 45]),
      ),
      0,
    )
  ) {
    return VotingAccount.PollAccount;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([112, 9, 123, 165, 234, 9, 157, 167]),
      ),
      0,
    )
  ) {
    return VotingAccount.VoteRecord;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "voting" },
  );
}

export enum VotingInstruction {
  CreatePoll,
  Initialize,
  Vote,
}

export function identifyVotingInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VotingInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([182, 171, 112, 238, 6, 219, 14, 110]),
      ),
      0,
    )
  ) {
    return VotingInstruction.CreatePoll;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237]),
      ),
      0,
    )
  ) {
    return VotingInstruction.Initialize;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([227, 110, 155, 23, 136, 126, 172, 25]),
      ),
      0,
    )
  ) {
    return VotingInstruction.Vote;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "voting" },
  );
}

export type ParsedVotingInstruction<
  TProgram extends string = "DXZ1gUBaHsj5cKAfRGZjqYigWF7wv1Js2aHKwKVYtUVA",
> =
  | ({
      instructionType: VotingInstruction.CreatePoll;
    } & ParsedCreatePollInstruction<TProgram>)
  | ({
      instructionType: VotingInstruction.Initialize;
    } & ParsedInitializeInstruction<TProgram>)
  | ({
      instructionType: VotingInstruction.Vote;
    } & ParsedVoteInstruction<TProgram>);

export function parseVotingInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedVotingInstruction<TProgram> {
  const instructionType = identifyVotingInstruction(instruction);
  switch (instructionType) {
    case VotingInstruction.CreatePoll: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VotingInstruction.CreatePoll,
        ...parseCreatePollInstruction(instruction),
      };
    }
    case VotingInstruction.Initialize: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VotingInstruction.Initialize,
        ...parseInitializeInstruction(instruction),
      };
    }
    case VotingInstruction.Vote: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VotingInstruction.Vote,
        ...parseVoteInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        { instructionType: instructionType as string, programName: "voting" },
      );
  }
}

export type VotingPlugin = {
  accounts: VotingPluginAccounts;
  instructions: VotingPluginInstructions;
  pdas: VotingPluginPdas;
  identifyAccount: typeof identifyVotingAccount;
  identifyInstruction: typeof identifyVotingInstruction;
  parseInstruction: typeof parseVotingInstruction;
};

export type VotingPluginAccounts = {
  globalState: ReturnType<typeof getGlobalStateCodec> &
    SelfFetchFunctions<GlobalStateArgs, GlobalState>;
  pollAccount: ReturnType<typeof getPollAccountCodec> &
    SelfFetchFunctions<PollAccountArgs, PollAccount>;
  voteRecord: ReturnType<typeof getVoteRecordCodec> &
    SelfFetchFunctions<VoteRecordArgs, VoteRecord>;
};

export type VotingPluginInstructions = {
  createPoll: (
    input: CreatePollAsyncInput,
  ) => ReturnType<typeof getCreatePollInstructionAsync> &
    SelfPlanAndSendFunctions;
  initialize: (
    input: InitializeAsyncInput,
  ) => ReturnType<typeof getInitializeInstructionAsync> &
    SelfPlanAndSendFunctions;
  vote: (
    input: VoteAsyncInput,
  ) => ReturnType<typeof getVoteInstructionAsync> & SelfPlanAndSendFunctions;
};

export type VotingPluginPdas = {
  globalState: typeof findGlobalStatePda;
  pollAccount: typeof findPollAccountPda;
  voteRecord: typeof findVoteRecordPda;
};

export type VotingPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function votingProgram() {
  return <T extends VotingPluginRequirements>(
    client: T,
  ): ExtendedClient<T, { voting: VotingPlugin }> => {
    return extendClient(client, {
      voting: <VotingPlugin>{
        accounts: {
          globalState: addSelfFetchFunctions(client, getGlobalStateCodec()),
          pollAccount: addSelfFetchFunctions(client, getPollAccountCodec()),
          voteRecord: addSelfFetchFunctions(client, getVoteRecordCodec()),
        },
        instructions: {
          createPoll: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCreatePollInstructionAsync(input),
            ),
          initialize: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeInstructionAsync(input),
            ),
          vote: (input) =>
            addSelfPlanAndSendFunctions(client, getVoteInstructionAsync(input)),
        },
        pdas: {
          globalState: findGlobalStatePda,
          pollAccount: findPollAccountPda,
          voteRecord: findVoteRecordPda,
        },
        identifyAccount: identifyVotingAccount,
        identifyInstruction: identifyVotingInstruction,
        parseInstruction: parseVotingInstruction,
      },
    });
  };
}
